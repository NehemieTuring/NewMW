spring:
  application:
    name: mutuelle-web
  datasource:
    url: jdbc:mysql://localhost:3306/mutuellen?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
        globally_quoted_identifiers: true
  jackson:
    serialization:
      fail-on-empty-beans: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      
server:
  port: 8080
  servlet:
    context-path: /api

jwt:
  secret: ${JWT_SECRET:mySecretKeyForMutuelleWebApplication2024VeryLongAndSecureWithExtraLengthToMeet512BitRequirement}
  expiration: 86400000

chat:
  api:
    url: ${CHAT_API_URL:https://api.stream-chat.com}
    key: ${CHAT_API_KEY:your-api-key}

logging:
  level:
    com.mutuelle: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web.socket: DEBUG

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
